def my_function(base,exponent):
    result=1
    for i in range(exponent):
        result*=base
    print("power number =",result)
base=int(input("enter the base value:"))
exponent=int(input("enter the exponent value:"))
my_function(base,exponent)

output:
enter the base value:4
enter the exponent value:2
power number = 16
